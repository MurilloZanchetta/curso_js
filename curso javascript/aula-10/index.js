// o que aprendemos.

const nome = 'Mu'   
const nome1 = "Mu"  
const nome2 = `Mu`

// 3 métodoss paara chamar string

const num1 = 10
const num2 = 10.78

// numeros inteiros e com virgulass são
// considerados number

let nomeAluno; //undefined -> não aponta pra local nenhum da memória
const ssobrenomeAluno = null; //Nulo -> não aponta pra local nenhum na memória
const aprovado = true // Boolean = true, false (lógico)


let a = 4;
const b = 8;
console.log(a, b)

a = 56
console.log (a, b)